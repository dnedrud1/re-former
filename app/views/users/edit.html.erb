<%= form_for @user do |f| %>

  Username:<%= f.text_field :username, :value => "Jon Smith" %>
  Email:<%= f.text_field :email, :value => "example@example.com" %>
  Password:<%= f.password_field :password %>
  <%= f.submit "Submit" %>
  
<% end %>